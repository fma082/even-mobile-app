# Copilot LLM — NOT wired yet (see services/copilot.ts).
# Copy to .env.local (gitignored) and fill in. Never commit real keys.
# Both OpenRouter and Groq expose an OpenAI-compatible /chat/completions endpoint.
COPILOT_BASE_URL=https://openrouter.ai/api/v1
COPILOT_MODEL=qwen/qwen-2.5-72b-instruct
COPILOT_API_KEY=
